name: tekstiandmete_kvaliteet_dbt
version: "1.0.0"
profile: tekstiandmete_kvaliteet_dbt

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

seeds:
  tekstiandmete_kvaliteet_dbt:
    +schema: marts

models:
  tekstiandmete_kvaliteet_dbt:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
